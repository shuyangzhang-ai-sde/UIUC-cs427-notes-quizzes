8.6 Parameterized Tests: Complex Parameters

​This is a lecture video on Parameterized Tests: Complex Parameters. ​By the end of this video, ​you will be able to explain the implicit conversion from string to ​a target type when supplying arguments sources as string literals. ​Write a parameterized test with parameters ​whose argument object values are converted from string literals. ​Write a parameterized test with ​the ArgumentsAssessor parameter and with the construction of objects inside the test. ​Write a parameterized tests with parameters annotated with custom aggregators, ​and construct arguments objects inside the custom aggregators. ​When we use value source or other annotations to ​indicate the source of the arguments to be string literals at runtime, ​when we use the execution engine to run the parameterized test, ​we carry out implicit conversion from such string literals, ​to the target parameter type. ​For example, if our target parameter type is Boolean, ​then we convert string value true to be the Boolean value true. 
​If the type is character, ​then we convert a single characters in ​the string literals to be the characters that we want. ​Similarly for integers, we convert the integer string to the integer values, ​and for Enumeric subclass we convert the string for indicating the constants defined on ​the Enumeric type such as a TimeUNIT.SECONDS converted from seconds string value. ​For java.io.File, we can convert the string values indicating the file name ​into a file object by putting the string value ​as argument value for the constructor or for file. ​We can also carry out Fallback string-to-object conversion, ​by converting string literals to the target parameterized type by using such mechanism. ​As our example, we use a value source to specify ​a string "42 cats" as the source for the argument values. ​Given that our target parameter type is book then, ​our execution engine we'll look for ​applicable factory methods being defined on our target parameter type book. ​If you look for a non private method being defined on ​the target parameter type and such method must be static. 
​These applicable factor measure must have a single string argument ​and the return of such applicable factor measures must be the target parameter type. ​In addiction, the execution engine will also look for ​non-private constructor from the target parameterized type with a single stream argument. ​If both the applicable factory constructor ​and the applicable factor method can be found on the target parameter type, ​then the factory method will be selected, ​not the applicable factory constructor. ​If multiple applicable factory measures are found on the target parameter type, ​then none of these factory methods will be applied. ​We can also carry out explicit conversion by ​annotating the parameter of the parameterized test with ​such annotation indicating the class that we are going to use to find out ​the conversion measures for converting from the source to the target parameter type. ​As you can here, ​we can find out two string argument converters as the specified converter class. ​Then we look for such converter method for converting ​the source as specified with the source of the argument. 
​Then, we convert the object to ​the target type as the argument here as a string type. ​Then, the return value of such a conversion method will be used as the argument values. ​We can also specify our parameters tests with parameter type argument assessor. ​By using these carefully design argument type, ​we can invoke getString or get to allow us to convert ​individual string values being specified in ​the argument source to our target argument types being used ​within the body of the parameterized tests for us to ​construct the needed object type such as a person in this example. ​For each of the four values of the argument source, ​we get the first one being string and the second one also strings. ​But for the third argument, ​we convert it to the gender class and the first one converted to local date. ​These for argument types are ​the needed argument types for the constructor of person class. 
​Instead of constructing the objects that will be ​needed in order to test different behaviors within the parameterized test, ​we can specify the construction of the target objects outside of the parameter test. ​But instead, we still parse in ​the target argument type as a person for the example we have seen. ​However, we annotate these argument types with aggregate with as the specific class name ​that we are going to look for the conversion or aggregation of ​the individual string values into the target object type as a person. ​In particular, we see that the person ​aggregator being defined as a public class and then we look for ​non private methods for returning ​the target type person and with argument assessor type and ​the parameter contexts for retrieving ​the individual argument values ​from the string literals being listed in the argument source.



1. True or False

题目：
To support the @CsvSource, JUnit provides conversions to convert String to target types.

选项：

True ✅
False ❌

✔ 正确答案：True

解释：
JUnit 5 的参数化测试会自动把 String 转换为目标类型（int、double、enum 等）。

2. Multiple Choice

题目：
Which of the following types can be converted by implicit conversion?

选项：

Float ❌
Short ❌
Enum subclass ❌
All ✅

✔ 正确答案：All

解释：
JUnit 的 implicit conversion 支持：

primitive / wrapper（float, short）
enum
所以是 All
3. Code Question
📦 代码块（完整提取）
@ParameterizedTest
@ValueSource(strings = "42 Cats")
void testWithImplicitFallbackArgumentConversion(Student s) {
    assertEquals("Jane", student.getName());
}

题目：
What type of conversion does it use?

选项：

Implicit Conversion ✅
Explicit Conversion ❌

✔ 正确答案：Implicit Conversion

解释（很重要⚠️）
没有使用：

@ConvertWith

JUnit 会：

尝试内置转换
fallback（constructor / factory）

👉 仍然属于 Implicit conversion

4. True or False

题目：
Explicit conversion should be used within @ConvertWith.

选项：

True ✅
False ❌

✔ 正确答案：True

解释：
@ConvertWith 就是专门用来写 custom converter（显式转换）

5. True or False

题目：
We can use @AggregateWith to specify a custom aggregator.

选项：

True ✅
False ❌

✔ 正确答案：True

解释：
@AggregateWith 用于：
👉 把多个参数组合成一个对象（比如 CSV → object）